import { LoadingState } from '@/src/design-system/components/LoadingState';

export default function SystemSettingsLoading() {
  return <LoadingState variant="spinner" label="Loading notification settings..." />;
}
